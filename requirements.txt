# Verified training stack (Linux x86_64, Python 3.12, CUDA 12.8).
# Install PyTorch first, then install this file with --no-build-isolation so
# flash-attn can build against the installed PyTorch/CUDA runtime.
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
vllm==0.17.0
flash-attn==2.8.3
ray[default]==2.54.0
transformers==4.57.1
tokenizers==0.22.2
huggingface-hub==0.36.2
safetensors==0.7.0
sentencepiece==0.2.1

# verl training dependencies.
accelerate==1.13.0
codetiming==1.4.0
datasets==4.8.3
dill==0.4.1
hydra-core==1.3.2
omegaconf==2.3.0
liger-kernel==0.7.0
numpy>=1.26,<2.3
pandas==3.0.1
peft==0.18.1
pyarrow==23.0.1
pybind11==3.0.2
pylatexenc==2.10
tensordict==0.10.0
torchdata==0.11.0
wandb==0.25.1
packaging>=25.0,<26.0
uvicorn==0.42.0
fastapi==0.135.1
latex2sympy2-extended==1.11.0
math-verify==0.9.0
tensorboard==2.20.0
nvtx==0.2.15
einops==0.8.2
ninja==1.13.0

# Reproduction tests.
pytest==9.0.2
pytest-asyncio==1.3.0
